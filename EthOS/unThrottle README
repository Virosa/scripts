#Installations Instructions:

cd /home/ethos/
echo 'sudo /home/ethos/unThrottle.sh' > /home/ethos/custom.sh
echo 'exit 0' >> /home/ethos/custom.sh
curl vi.zenithnet.org/download/unThrottle.sh
chmod +x unThrottle.sh

#Will unthrottle the local rig every 15 mins.

#Purpose : In 1.0.5, throttling was implemented. This clean all the files so that throttling can kick in again 
#and then restore clocks/fan speeds from config.

#-Virosa
